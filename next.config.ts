import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // App Router is enabled by default in Next.js 13+
  // next-themes works without any special config
}

export default nextConfig
